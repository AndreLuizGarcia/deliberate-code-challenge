import styled from 'styled-components'

export const Wrapper = styled.header`
  display: flex;
  align-items: center;

  img {
    width: 50px;
  }

  nav {
    display: flex;
    gap: 8px;

    a {
      text-decoration: none;
      color: white;
    }
  }
`
